# Set the canary value

Change only `canary_value.py` so `VALUE` is the integer `2`.

## Acceptance criteria

- `python3 canary_test.py` passes.
- `canary_test.py` is unchanged.
- No files outside `canary_value.py` and this ticket's completion metadata are changed.
